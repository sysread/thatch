[tools]
bun = "1.3.14"

[env]
THATCH_DB_PATH = { value = "{{ config_root }}/thatch.db", optional = true }

[tasks.test]
description = "Run the test suite"
run = "bun test"

[tasks.test-watch]
description = "Run tests in watch mode"
run = "bun test --watch"

[tasks.cli]
description = "Run the thatch CLI (list, show, search, forget)"
run = "bun run bin/thatch"

